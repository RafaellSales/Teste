import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  empty: {
    padding: 16,
    borderRadius: 8,
    alignItems: "center",
    justifyContent: "center",
  },
  emptyText: {
    fontSize: 20,
    fontWeight: "bold",
    textAlign: "center",
    color: "#c03",
  },
});
